# 变更日志

本文件记录每个发布版本的用户可见变更与修复。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
详细的开发过程与验收记录见 `ROADMAP.md`。

---

## [1.2.0] - 2026-09-19

Boot 刷入槽位选择。修复「引导刷入」页面刷 boot 时没有 B 槽入口的问题。

### 新增
- **刷入 Boot / init_boot 支持 A/B 槽位选择**：分区与槽位拆成两个下拉，
  由程序拼装最终分区名（`boot` + `B` → `boot_b`），不再要求用户手打带后缀的名字。
- **「检测分区与槽位」按钮**：一次 `getvar` 扫描列出设备上真实存在的可写分区
  （`boot_a`、`init_boot_b`、`vbmeta_a` 等）与不存在的，选镜像前即可确认。
- **`src/slot_resolver.js`**：分区名拼装与校验的单一实现，主进程与渲染进程共用，
  配 12 项单元测试覆盖（含 `vbmeta` 以 `a` 结尾但并非槽位后缀这类边界）。

### 修复
- **刷入 Boot 没有槽位入口**：按钮说明写着"选择 A/B 槽位对应的 boot 镜像后写入"，
  但参数弹窗只有一个自由文本的「目标分区」输入框，B 槽无法选择。
  现在分区为下拉（boot / init_boot / vendor_boot / vbmeta / dtbo / recovery / 自定义），
  槽位为独立下拉（当前活动槽位 / A / B / 不带后缀）。
- **按钮 `data-partition` 覆盖用户选择**：`runAction` 中该属性在参数弹窗之后回填，
  会把已解析好的 `boot_a` 覆盖回 `boot`。现改为仅在弹窗未产出分区时回填。
- **写向非活动槽时无提醒**：现在明确提示目标分区属于哪个槽、
  以及写入后需要切换启动槽位才会生效，避免"刷了没反应"或覆盖备槽失去回滚能力。
- **槽位读取失败时静默降级**：选「当前活动槽位」但读不到 `current-slot` 时不再
  默认猜一个槽写入，而是拦下并要求手动选择。
- **单槽机型选槽位会静默失败**：现在会在写入前拦下并说明设备不是 A/B 机型。

### 工程
- `audit-actions` 新增两项校验：注册表声明的动作必须真实存在；
  危险动作的通用确认链路（界面确认函数 + 主进程 `riskConfirmed` 强制校验）必须完整。
- 界面动作的「已登记」判定改为从 `main.js` switch 与 `renderer.js` 本地处理器
  自动抽取，不再依赖手写白名单——此前新增主进程动作时漏同步会误报 P1。
- 测试 55 → 67 项。

---

## [1.1.0] - 2026-09-17

界面风格扩展版。功能与 1.0.0 完全一致，新增 6 套可选界面主题。

### 修复
- **连接状态文字在切换主题后看不清**：`.connection-state` 与 `.brand-copy` 位于
  `aside.sidebar.app-header` 内，而 `.app-header` 把该元素背景覆盖为浅色的
  `--surface-raised`；此前文字用 `--sidebar-text`（白色），浅色主题下变成白字白底。
  现改用正文色 `--text` / `--text-muted`。
- **导航未选中项文字对比度不足**：深色主题下用 `--text-secondary` 在白底侧栏上
  对比度仅 2.22（WCAG AA 要求 ≥ 4.5）。新增 `--nav-text` 变量按主题取值，
  并修正 `.app-header .nav-item` 两处定义（后一处此前会覆盖前一处）。
- **导航选中项与徽章的硬编码白色**：`color: #fff` 改为 `var(--accent-on)`，
  曜石霓虹等亮色强调下不再出现白字压亮底（对比度 2.98 → 达标）。
- **墨玉青瓷强调色过亮**：`--accent` 由 `#2d8a74` 调整为 `#26745f`，
  白字对比度由 4.20 提升到 5.61。

### 新增
- **6 套界面风格**，右上角调色板图标点击循环切换，选择结果本地保存、重启保持：
  | 主题 | 名称 | 明暗 | 特点 |
  |------|------|------|------|
  | `graphite` | 深空石墨 | 深色 | 原有深色，中性石墨灰 + 薄荷绿 |
  | `indigo` | 午夜靛蓝 | 深色 | 深海军蓝 + 靛蓝，圆角与投影更大 |
  | `obsidian` | 曜石霓虹 | 深色 | 纯黑 + 青色，侧栏紫蓝渐变，科技感最强 |
  | `light` | 清亮浅色 | 浅色 | 原有浅色，冷灰底 + 绿色强调 |
  | `sand` | 暖云米白 | 浅色 | 米白纸面 + 深棕侧栏 + 琥珀点睛 |
  | `jade` | 墨玉青瓷 | 浅色 | 低饱和灰绿 + 玉色，圆角最小 |
- **主题审计 `audit:themes`** 并接入 CI：校验每套主题的 33 个变量是否齐全，
  以及 `renderer.js` 的主题列表与 `styles.css` 的定义是否一一对应。
- `design/` 目录：界面方案对比页、可切换预览页与真实应用截图（不进入安装包）。

### 变更
- 侧栏选中态、悬停态、品牌文字与连接状态文字由硬编码颜色改为 CSS 变量，
  切换主题时不再残留旧配色。
- 设备主卡片改为使用 `--hero-bg` / `--hero-line`，支持主题自定义渐变。
- 主题切换按钮由「浅色/深色」二选一改为 6 套循环，按钮提示显示当前风格名。
- 旧的 `localStorage.theme` 取值 `light` / `dark` 自动映射到 `light` / `graphite`，升级不丢设置。

---

## [1.0.0] - 2026-09-17

首个正式版。此前版本号为 20.x 的内部迭代系列，功能已稳定，本版统一为正式发布版本号。
V20.5.x 系列（含投屏会话、无线配对、应用与存储管理、危险操作审计等）的全部内容
均已包含在本版中，明细见下方历史条目。

### 修复
- **ADB 设备详情被截断**：`parseAdbDevices` 用 `split(/\s+/, 3)` 取三段，
  但 JS 中该参数是**截断上限**，detail 只保留 `product:xxx`，丢失 `model` 与
  `transport_id`。现改为按位置切分，detail 保留完整剩余内容。
- **固件解析的 erase 判定改为严格布尔**：此前 `parseFirmwareXml` 按真值判断
  `allowErase`，传 `1` 或 `'true'` 也会放行清除数据，现要求严格 `=== true`。
  现有调用点本就传 `payload.allowErase === true`，行为不变，此处是防御下沉。
- **`shellArg` 对空值更稳健**：`shellArg(null)` 此前会产生字符串 `'null'` 并送进
  设备 shell，现按空命令处理。
- **固件刷机「选择刷机包」失败**：`parseFirmwareXml` 错误跳过了 `<step>` 节点，
  而 Motorola/Lenovo 官方刷机包的全部命令都写在 `<step operation=...>` 上，
  导致解析出 0 条命令并报「XML 中没有解析到可执行的 Fastboot 命令」。
  现已正确解析，实测 `XT2335-3`（penang）刷机包可解析出 27 条命令。
- **Root 相关功能大面积失效**：`su -c` / `sh -c` 传入的多词命令未加引号，
  adb 拼接参数后远程 shell 会重新按空格与 `;` 拆分，命令被截断。
  已统一用 `shellArg()` 包裹全部 23 处调用。
- **状态轮询卡死**：`getStatus` 的 Root 探测没有超时，无 root 设备上会长时间阻塞，
  导致状态栏一直显示「检测中」。已为全部探测命令加上超时。

### 变更
- **固件刷机清除语义收紧**：`erase` 步骤改为显式 opt-in（默认跳过），
  只有带 `data-allow-erase="true"` 的完整刷机入口才执行清除。
- **多设备刷机安全**：固件刷机现在绑定目标序列号，所有 fastboot 命令带 `-s <serial>`，
  避免连接多台 Fastboot 设备时刷错机器。
- **危险动作清单统一**：`DANGEROUS_ACTIONS` 与 `FEATURE_DANGER` 已完全一致，
  补齐 `firmware-flash`、`uninstall-packages-batch`、`freeze-apps-user0-batch`、
  `clear-storage-cache`、`configure-denylist-user-apps`。

### 性能
- `getStatus` 增加并发保护（进行中的请求直接复用），避免 7 秒轮询与手动刷新叠加堆积。
- 状态采集由 6 次 `getprop` 往返合并为 1 次 `adb shell getprop` 批处理。
- adb / fastboot 版本号探测结果缓存，不再每次轮询重复执行。
- 安装包体积下降约 **240 MB**：合并 Tea 模板中内容完全相同的 A/B 槽位镜像
  （释放 480 MB 原始资源，960 MB → 480 MB）。

### 工程
- 新增 `src/actions.registry.js` 作为动作元数据唯一来源，主进程与界面共用同一份定义，
  消除此前 6 个表重复维护、容易漂移的问题。
- **解析逻辑抽为可测模块**：固件 XML 解析迁到 `src/firmware_parser.js`，
  adb / fastboot 设备列表解析迁到 `src/adb_parser.js`。两者不依赖 Electron，
  可脱离应用直接单元测试。
- **新增 19 项回归测试**（`tests/v1.0.0-regression.test.js`），覆盖已经实际出过问题的
  边界：`shellArg` 引号转义、`parseFirmwareXml` 的 `<step>` 解析与 `erase` 严格 opt-in、
  `parseAdbDevices` 的 detail 完整性、无线地址范围校验。测试总数 35 → 54。
- **新增 `scripts/ci.ps1` 与 `npm run ci`**：依次执行语法检查、单元测试、
  生产依赖审计、7 项契约审计、资源完整性校验。审计脚本的 P1 缺口记为警告，
  P0 才判定失败。`release.ps1` 第一步即调用它，发布不会漏跑校验。
- 重构 `scripts/release.ps1`：打包前先抢救 `dist/` 既有安装包（electron-builder 会清空
  输出目录），新增 `-SkipCi` / `-SkipBuild` / `-SkipInstall` / `-SkipVerify` / `-NoGit` 开关，
  发布完成后自动提交并打 tag。
- 新增 `resources/MANIFEST.md` 与 `scripts/verify-resources.ps1`：
  运行时二进制资源不入库，改用清单 + 校验脚本管理。
- 历史记录文件按大小滚动（2 MB × 3 份），此前 `command-history.jsonl` 无上限增长。
- 新增主进程 `uncaughtException` / `unhandledRejection` 落盘，崩溃不再无痕迹。
- 仓库纳入 Git 版本管理，补齐 `.gitignore` 与发布脚本的 UTF-8 BOM 处理。
- **仓库清理**：早期版本曾把约 456 MB 二进制资源纳入 Git 跟踪，
  导致 `.gitignore` 失效、仓库异常膨胀。现已将这些资源移出版本控制并重建仓库，
  `.git` 从 456 MB 降至约 273 KB；`dist/` 中累积的历史安装包同步清理（释放 5.6 GB），
  历史版本仍完整保留在桌面归档目录。

### 已知项
- 生产依赖（`npm audit --omit=dev`）**无已知漏洞**。
  开发依赖有 16 项告警，全部来自 `electron` / `electron-builder` 构建工具链的
  传递依赖（`tar` 等），不进入安装包；自动修复需跨主版本升级 Electron（41 → 44），
  待单独评估后处理。

---

## [20.5.8]

### 新增
- 投屏会话生命周期管理（`mirror-session`），支持会话跟踪、重复启动拒绝与停止。
- 新增 `npm run audit:mirror-session` 后台审计。

### 修复
- 显示设置修改后立即生效，无需重启。

---

## [20.5.7]

### 新增
- 无线 ADB 支持 Android 11+ 配对码与配对端口，配对与连接端口分离。
- 新增 `npm run audit:wireless-pair` 与 mock 契约测试。

---

## [20.5.6]

### 新增
- 无线投屏前置于严格校验：IPv4 每段范围、投屏端口范围。
- scrcpy 显式绑定 USB serial 或 `IP:端口` 无线 serial。

---

## [20.5.5]

### 修复
- UI 状态与后端结果一致性：成功、失败、警告、处理中状态统一。
- 应用/文件排序、多选、筛选、滚动和刷新不再丢失上下文。

---

## [20.5.4]

### 变更
- 危险操作安全边界审计：刷写、解锁、卸载、清数据、清理媒体和修改显示设置
  全部走二次确认；取消、设备不匹配、权限不足和备份失败都会阻止写入。

---

## [20.5.3]

### 新增
- 任务中心接入：任务开始、进度、完成、失败和超时的状态转换可视化。
- 全局动作锁与 409 冲突反馈，失败计数不会覆盖任务结果。

### 待办
- 任务取消、断点恢复和同路径传输冲突尚未实现。

---

## [20.5.2]

### 修复
- 设备状态、应用版本/大小/冻结状态和存储统计改为字段级解析校验。
- 空值、权限不足、目录不可访问和部分结果的提示更准确。
- 文件路径规范化，`..` 穿越与特殊字符边界测试覆盖。
- 导出目标保留公共存储相对目录，并通过唯一目标名避免同名文件覆盖。

---

## [20.5.1]

### 修复
- ADB/Fastboot 命令真实性审计：统一注入所选 serial，多设备场景不会落到默认设备。
- 校验参数、退出码、stderr 和超时是否正确传递。

---

## [20.5.0]

### 新增
- 功能与动作清单审计：校验 `ACTION_IDS`、后端 handlers 与界面动作注册一致。
- 新增 `npm run audit:actions` 后台审计。

---

## [20.4.0]

### 新增
- 应用列表支持名称、占用空间、类型和状态排序。
- 多选与全选，支持批量导出 APK、卸载用户应用、冻结和解冻。
- 存储扫描显示照片、视频、缓存、下载残留的文件数量和空间占用。
- 清理前提供扫描结果与具体目录，清理后显示删除数量、释放空间及失败项。

---

## [20.3.2] 及更早

UI 基线版本，固定浅色左侧导航布局。此前的 Python 版本历史见旧仓库记录。
