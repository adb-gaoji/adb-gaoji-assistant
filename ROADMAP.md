# ADB搞机助手升级路线图

## 当前基线

- **已发布正式版：V1.0.0**（由 V20.5.9 收敛为正式版本号，已覆盖安装）
- 仓库状态：已重建为干净仓库（42 文件 / `.git` 约 273 KB / tag `v1.0.0`）
- 测试基线：54 项（35 项契约测试 + 19 项回归测试）
- CI：`npm run ci` 依次跑语法、测试、生产依赖审计、7 项契约审计、资源校验
- 固定界面：浅色左侧导航；保留当前按钮尺寸、圆角、卡片、表格和弹窗风格
- 固定发布流程：CI → NSIS 打包 → 桌面版本归档 → 覆盖安装 → 版本校验 → 单实例验证 → git 提交打 tag
- 测试边界：不自动执行真实刷机、解锁、分区写入、恢复出厂或不可恢复的手机数据删除

## V1.0.0 正式版（已完成）

### 已完成

- [x] 修复固件刷机 XML 解析跳过 `<step>` 导致选包失败
- [x] 修复 `su`/`sh -c` 引号丢失导致 Root 功能失效（23 处）
- [x] 修复 `parseAdbDevices` 设备详情被 `split` 截断
- [x] 固件刷机绑定目标序列号；`erase` 严格 opt-in
- [x] `getStatus` 并发保护 + `getprop` 批处理 + 版本缓存
- [x] 动作元数据统一到 `src/actions.registry.js`
- [x] 解析逻辑抽为可测模块（`firmware_parser` / `adb_parser`）
- [x] 新增 19 项回归测试，覆盖上述已修问题
- [x] 新增 `scripts/ci.ps1` 并接入发布流程
- [x] 合并 Tea 模板 A/B 槽位冗余镜像（安装包 623.9 MB → 524.9 MB）
- [x] 仓库清理：456 MB → 273 KB；`dist/` 释放 5.6 GB

### 已知缺口（CI 警告，不阻断发布）

- [ ] **任务取消**：任务中心只能等待完成，无中途取消（`audit:tasks` 报 P1）
- [ ] **开发依赖告警**：`electron` / `electron-builder` 构建链 16 项 CVE，
      修复需跨主版本升级 Electron 41 → 44，需单独评估回归成本

### 后续候选（按投入产出排序）

- [ ] **拆 `renderer.js`**（82 KB / 1682 行）为按域模块，降低改动成本
- [ ] **统一动作分发**：`action_handlers.js` 与 `main.js` 的 switch 目前是两套并行路径
- [ ] **任务断点恢复**：长任务中断后可续，而非从头执行
- [ ] **操作历史界面化**：`action-history.jsonl` 已有数据，可做成可查询的操作记录
- [ ] **低频工具按需下载**：`bundled-tools/` 占 117 MB，可改为首次使用时获取

## V20.4.0 应用与存储管理专业化

### P0 必须完成

- [x] 应用列表增加名称、占用空间、类型和状态排序
- [x] 应用列表增加多选和全选，支持批量导出 APK、卸载用户应用、冻结和解冻
- [x] 危险批量操作确认中显示设备、选中数量和应用清单摘要
- [x] 存储扫描显示照片、视频、缓存、下载残留的文件数量和空间占用
- [x] 清理前提供扫描结果和具体目录，清理后显示删除数量、释放空间及失败项
- [x] 扫描、批量导出和批量操作接入任务中心，显示进度并阻止重复任务
- [x] 应用列表滚动、筛选、切换视图和刷新过程保持当前位置，不发生布局跳动
- [x] 为 V20.4.0 增加单元测试和 UI 合同测试

### 增量记录

- 2026-08-07：完成应用排序。支持名称正序/倒序、空间升序/降序、用户/系统类型优先、冻结/正常状态优先；列表行同步显示应用占用空间。`npm test` 13 项通过；真机只读验证 378 个应用，空间降序正确、当前选择保留、滚动与工具栏布局正常。
- 2026-08-07：完成应用多选、全选当前列表和批量导出、卸载、冻结、解冻；跨筛选保留选择，批量卸载自动排除系统应用，冻结与解冻自动过滤不适用状态。危险确认显示设备、应用数量和名称摘要，后端顺序执行并返回成功、失败清单。`npm test` 14 项通过；真机只读验证 378 个应用、3 项跨筛选选择、35 项全选、滚动位置保持、工具栏无溢出，卸载与冻结确认均取消且未执行。
- 2026-08-07：完成公共存储只读扫描。照片、视频和下载残留按真实文件数量与字节数统计，应用缓存显示真实占用和有缓存的应用数量，并展示对应扫描目录。`npm test` 15 项通过；真机验证设备存储 23.7 GB / 106 GB、375 个应用占 6.59 GB、154 个应用缓存占 41.4 MB，当前照片、视频和匹配残留均为 0 个；五张分类卡片无横向溢出，未执行任何清理。
- 2026-08-07：完成存储清理明细与结果反馈。确认弹窗显示当前设备、扫描数量、占用空间和具体目录；照片、视频和垃圾清理先枚举文件后逐个删除，返回删除数、预计释放字节和失败路径；缓存清理由前后扫描计算释放空间。`npm test` 15 项通过；真机只读验证照片、垃圾确认弹窗并全部取消，未执行清理。
- 2026-08-07：完成任务执行中心接入。应用列表扫描、存储扫描、批量导出及批量包操作均创建任务记录；批量操作按`[任务进度] 当前/总数 包名`更新进度，任务中心保留最近 20 条历史，并在任务运行期间阻止重复扫描和冲突操作。`npm test` 16 项通过；真机只读验证应用扫描 379 个应用、存储扫描 23.9 GB / 106 GB，任务中心可见两条扫描历史；未执行批量导出、卸载、冻结、清数据或媒体删除。
- 2026-08-07：完成应用列表位置保持。新增显式滚动状态，列表重绘后按内容高度恢复滚动位置；搜索、筛选、排序、切换应用/存储视图和刷新不会清空用户位置，短列表仅将位置限制到可用范围。合同测试覆盖状态保存、恢复和边界限制；`npm test` 17 项通过。
- 2026-08-07：完成 V20.4.0 测试门禁。新增包查询解析器单元测试、无效序列号不调用 ADB 的安全测试，以及任务中心、应用管理、存储分类和日志工具的 UI 合同测试；`npm test` 20 项通过。

### 发布验收

- [x] `npm test` 全部通过
- [x] 真机只读验证应用数量、空间统计和滚动区域
- [x] 不执行真实批量卸载、清数据、照片或视频删除测试
- [x] 执行 `npm run release:install`
- [x] 安装包归档到桌面 `ADB搞机助手` 文件夹并保留旧版本
- [x] 已安装版本、安装包哈希和单实例通过验证
- [x] 验证完成后没有 ADB搞机助手或 Electron 测试进程残留

- 2026-08-07：V20.4.0 发布验收完成。`npm test` 20 项通过；`npm run release:install` 构建并覆盖安装成功，安装版本 `20.4.0`，桌面归档 `ADB搞机助手_V20.4.0_安装包.exe`，SHA-256 `B50DBB6C96F9EFD1ADF069CF4BE6B32683A2CA4A3DBA898D1E050FD568FC29AB`；单实例验证通过，旧版本包保留，验证后无项目 Electron/ADB 进程残留。

## V20.5.x 全功能真实性审计（10 个小版本）

目标：先验证每个动作是否真实、是否绑定正确设备、是否有可观测结果和失败处理，再决定是否新增功能或提升版本号。所有审计在后台运行，不启动 Electron 窗口，不执行刷机、解锁、分区写入、卸载、清数据或删除手机媒体。

### V20.5.0 功能与动作清单审计

- [x] 校验 `ACTION_IDS`、后端 handlers 与界面动作注册一致
- [x] 生成后台审计报告，记录未绑定所选序列号、命令插值和文件导出边界缺口
- [x] 修复审计报告中的 P0 设备绑定问题（V20.5.1）

验收：`npm run audit:actions` 无窗口输出 JSON；`npm test` 合同测试通过；审计发现必须进入路线图，不得以“测试通过”掩盖。

### V20.5.1 ADB/Fastboot 命令真实性审计

- [x] 修复首轮审计命中的 ADB 动作：统一注入所选 serial，并验证多设备场景不会落到默认设备
- 校验 ADB/Fastboot 参数、退出码、stderr 和超时是否被正确传递
- 用 mock 覆盖重启、安装、导出、修复和 Fastboot 读写命令；只读真机只验证查询命令

### V20.5.2 设备、应用与存储数据真实性审计

- 对设备状态、应用版本/大小/冻结状态和存储统计做字段级解析校验
- 校验空值、权限不足、目录不可访问和部分结果的提示准确性
- [x] 对文件路径做规范化、`..` 穿越和特殊字符边界测试
- [x] 导出目标保留公共存储相对目录，并通过唯一目标名避免同名文件覆盖

### V20.5.3 任务中心、并发和进度审计

- [x] 验证任务开始、进度、完成、失败和超时结果的状态转换
- [x] 验证全局动作锁、409 冲突反馈和失败计数不会覆盖任务结果
- [ ] 任务取消、断点恢复和同路径传输冲突仍待实现与验证
- 校验历史记录与实际命令结果一致

### V20.5.4 危险操作安全边界审计

- [x] 逐一检查刷写、解锁、卸载、清数据、清理媒体和修改显示设置的二次确认
- [x] 确认取消、设备不匹配、权限不足和备份失败都会阻止写入
- [x] 危险动作只保留静态/mock/只读验证，不做真实破坏性测试

### V20.5.5 UI 状态、筛选、滚动和响应式审计

- [x] 检查成功、失败、警告、处理中状态与后端结果一致
- [x] 验证应用/文件排序、多选、筛选、滚动和刷新不丢失上下文
- [x] 后台 DOM 合同检查，不弹出窗口

### V20.5.6 无线投屏连接前置审计

- [x] 严格校验 IPv4 每段范围和投屏端口范围
- [x] 无线 ADB 与无线投屏均先验证当前选择设备
- [x] scrcpy 显式绑定 USB serial 或 `IP:端口` 无线 serial
- [x] 增加 `npm run audit:wireless-cast` 和 mock 合同测试

### V20.5.7 无线 ADB 配对完善

- [x] 支持 Android 11+ 无线调试配对码与配对端口
- [x] 区分配对端口、连接端口和当前设备 serial
- [x] 覆盖非法配对码、非法端口和配对/连接命令顺序

### V20.5.8 投屏会话生命周期

- [x] 记录启动、运行、停止、异常退出状态
- [x] 阻止同一设备重复启动投屏会话
- [x] 提供投屏状态和停止入口

### V20.5.9 断线与重连

- 识别 ADB 断线、网络切换和 scrcpy 退出原因
- 按原设备 serial 恢复，不切换到其他设备
- 重连失败显示可执行的处理建议

### V20.6.0 投屏质量配置

- 码率、分辨率、帧率和控制模式范围校验
- 保存用户配置并支持恢复默认值
- 配置结果写入任务和日志记录

### V20.6.1 多设备投屏隔离

- 每个会话绑定独立 serial、端口和 scrcpy 进程
- 多设备同时操作时互不抢占
- 设备移除后清理对应会话状态

### V20.6.2 输入与音频能力探测

- 读取设备 Android 版本、编码器和音频能力
- 不支持控制或音频时明确提示原因
- 只读探测，不修改设备配置

### V20.6.3 投屏诊断中心

- 汇总 ADB、网络、scrcpy、编码器和断线日志
- 支持搜索、复制和导出诊断报告
- 报告绑定设备 serial 和会话时间

### V20.6.4 投屏资源与安装审计

- 检查 scrcpy、ADB、DLL、驱动和版本哈希
- 缺失资源时阻止启动并显示精确路径
- 校验打包后资源与源码版本一致

### V20.6.5 无线投屏全量回归与发布

- 完成 mock、真机只读、断线模拟和多设备回归
- 通过 `npm test`、打包、覆盖安装和单实例验收
- 保留桌面历史安装包并清理验证进程

### V20.5.0 审计记录

- 2026-08-07：后台动作审计脚本与合同测试完成。107 个动作全部有 handler，界面动作无未注册项；报告确认存在部分设备动作未绑定所选 serial、`shell -c` 字符串插值，以及文件导出同名覆盖和路径边界缺口。`npm run audit:actions` 已加入；`npm test` 22 项通过。未启动窗口，未执行任何危险操作。

### V20.5.1 审计记录

- 2026-08-08：修复首轮审计命中的 15 个 ADB 默认设备风险点。GMS、文件拉取、截图/录屏、显示设置、无线 ADB、Magisk 查询、关键分区备份和开机动画流程均先验证所选 serial，后续设备命令通过 `adbFor(payload, ...)` 注入 `-s SERIAL`。新增 mock 验证覆盖截图、DPI、GMS 修复及非法 serial；`npm test` 23 项通过。审计保留文件导出同名覆盖和路径边界两个 P1，`npm run audit:actions` 因此返回退出码 1，发布继续阻止。未启动窗口，未执行真实设备写入或破坏性动作。

### V20.5.2 审计记录

- 2026-08-08：修复手机文件导出边界。仅接受 `/sdcard/` 公共存储文件，拒绝 `..` 路径段、空路径和越界目标；导出目录保留手机端相对路径，特殊字符会转换为 Windows 可用文件名，同名文件通过唯一目标名处理。新增重复 basename 与路径穿越 mock 测试；`npm test` 24 项通过，`npm run audit:actions` 返回 0。未启动窗口，未执行真机导出或删除。

### V20.5.3 审计记录

- 2026-08-08：新增后台任务流审计 `npm run audit:tasks`。核心任务开始保护、进度上限、完成历史、ticker 清理、后端全局锁、409 冲突反馈和文件导出无效路径计数均通过；修复导出任务对无效路径的错误计数提示。`npm test` 25 项通过；任务审计返回退出码 1，仅保留 P1「任务取消能力未实现」。未启动窗口，未执行真实设备写入或破坏性动作。

### V20.5.4 审计记录

- 2026-08-08：完成危险操作边界审计。刷机、解锁、卸载、清数据、媒体清理、显示恢复、GMS 修改、批量冻结和开机动画写入统一由风险确认弹窗显示设备、模式和目标；确认后才附加 `riskConfirmed`，主进程会拒绝所有缺少该标记的危险 IPC 请求。新增 `npm run audit:danger` 无窗口静态审计与合同测试；未启动窗口，未执行真实设备写入或破坏性动作。
- 2026-08-08：V20.5.4 已完成发布验收。`npm test` 26 项通过；`npm run audit:actions` 与 `npm run audit:danger` 均通过；已构建并覆盖安装，桌面归档 桌面归档 `ADB搞机助手_V20.5.4_安装包.exe`，SHA-256 `B99B002667430A965F0EC3F587A5C0E3A8C9A61EB71AE566940445A04B960753`；安装版本 `20.5.4.0`，单窗口验证通过。验证结束后无 ADB搞机助手或 Electron 进程；保留系统 WinGet 平台工具的独立 ADB server。

### V20.5.5 审计记录

- 2026-08-10：完成后台 UI 状态合同审计。成功、警告、失败和处理中反馈、ARIA 状态区、应用筛选/排序/多选、刷新后选择保留、滚动位置恢复、存储反馈、左侧导航响应式断点及小屏表格滚动共 9 项检查通过。修正审计脚本中依赖源码字段出现顺序和旧侧栏宽度 `228px` 的两处误报，未修改界面布局；`npm run audit:ui-state` 返回 0，`npm test` 27 项全部通过。全程未启动 Electron 窗口，未执行真机写入或破坏性动作。

### V20.5.6 审计记录

- 2026-08-10：完成无线投屏连接前置审计。修复 IPv4 仅格式校验导致的越界地址问题；无线 ADB 和无线投屏统一验证当前选择设备；无线投屏启动 scrcpy 时显式绑定 USB serial 或 `IP:端口`，避免多设备落到默认设备。新增 `npm run audit:wireless-cast` 和 3 项 mock/静态合同测试；目标测试 3/3 通过，尚未执行真实无线连接或启动 Electron 窗口。

### V20.5.7 审计记录

- 2026-08-10：新增 Android 11+ 无线调试配对入口。配对请求严格区分 IP、配对端口、6 位配对码和可选连接端口；后端按 `adb pair IP:配对端口 配对码`、`adb connect IP:连接端口` 顺序执行并返回两个独立 serial。新增 `npm run audit:wireless-pair`、纯函数边界测试和 handler mock，目标测试 3/3 通过。未向真机发送配对命令，未启动 Electron 窗口。

### V20.5.8 审计记录

- 2026-08-10：完成投屏会话生命周期。scrcpy 启动记录 PID、设备 serial、状态和时间；同一设备重复启动返回 409；新增投屏状态和停止动作，进程退出会更新会话结果。通过假进程 mock 验证启动、重复保护和停止，新增 `npm run audit:mirror-session`；目标测试 2/2 通过，未启动真实 scrcpy 或 Electron 窗口。

### 版本原则

- 每个小版本只关闭一个审计主题，不重复堆叠功能
- 审计先于发布；存在 P0/P1 缺陷时不提升版本号、不覆盖安装
- 每次定时运行只做一个可验证增量，并记录命令、结果和未解决缺陷

## 后续原则

- 每次定时运行只推进一个可验证增量，并记录完成项和验证结果
- 修改前先列出预计文件，保持改动范围明确
- 版本验收全部完成后才提升版本号并执行发布流程
- 当前路线图完成后，再补充下一阶段计划，不进行无关 UI 重做
