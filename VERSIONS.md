╔══════════════════════════════════════════════════════════════════════════╗
║                      ADB搞机助手 · 版本升级说明                           ║
╚══════════════════════════════════════════════════════════════════════════╝

本文件由发布脚本在每次安装/更新后自动弹出。
所有历史版本的变更都记录在下方，最新版本排在最前面。


════════════════════════════════════════════════════════════════════════════════
  【1.2.0】 2026-09-19 · Boot 刷入槽位选择
════════════════════════════════════════════════════════════════════════════════

■ 本次升级重点
  「引导刷入」页面刷 boot 时，现在可以明确选择 A / B 槽位了。
  此前只能刷到当前槽位，B 槽没有入口。

■ 修复的问题
  · 刷入 Boot 没有槽位入口
      现象：按钮上写着"选择 A/B 槽位对应的 boot 镜像后写入"，
            但点进去只能填一个分区名，没有任何槽位选择，
            B 槽完全刷不了。
      原因：分区名靠用户手打。A/B 机型的分区在设备上带槽位后缀
            （boot_a / boot_b），但界面既不提示也不提供选择，
            打错一个字母要到刷写中途才报错——那时镜像已经传了一部分。
      现在：分区改成下拉（boot / init_boot / vendor_boot / vbmeta /
            dtbo / recovery / 自定义），槽位单独一个下拉
            （当前活动槽位 / A / B / 不带后缀），由程序拼装最终分区名。

  · 确认弹窗显示的分区名可能不是真实写入目标
      原因：按钮上的 data-partition 会在参数弹窗之后覆盖用户的选择，
            导致"选的是 boot_a、实际写入 boot"。
      现在：确认弹窗显示的是拼装完成的最终分区名，刷机前最后一步
            就能看出槽位选没选对。

  · 写错槽位没有任何提醒
      现在：写向非活动槽时会明确提示
            "目标分区 boot_b 属于槽位 B，而当前活动槽位是 A。
             写入后需要执行切换启动槽位才会从新槽启动"。

  · 单槽机型选了槽位会静默失败
      现在：设备读不到 current-slot 时，选槽位会被拦下并说明原因，
            而不是默默写一个不存在的分区。

  · 选"当前活动槽位"但读不到槽位时
      现在：拦下来要求手动选，不会默认猜一个 A 槽写进去。

■ 新增
  · 「检测分区与槽位」按钮：一次性列出设备上真实存在的可写分区
    （boot_a、init_boot_b、vbmeta_a 等）和不存在的，选镜像前就能确认。
  · 分区拼装规则抽成独立模块 slot_resolver.js，主进程与界面共用同一份实现，
    配 12 项单元测试覆盖（含 vbmeta 以 a 结尾但并非槽位后缀这类边界）。

■ 说明
  只改刷 boot / init_boot 这一条路径的交互，其它刷机流程（整机固件、
  Tea 制作、槽位切换）行为不变。

■ 工程
  · 动作审计新增两项校验：注册表里声明的动作必须真实存在；
    危险动作的通用确认链路必须完整。
  · 界面动作的"已登记"判定改为从源码自动抽取，不再依赖手写白名单。
  · 测试 55 → 67 项，CI 14 项全绿。


════════════════════════════════════════════════════════════════════════════════
  【1.1.0】 2026-09-17 · 界面风格扩展版
════════════════════════════════════════════════════════════════════════════════

■ 本次升级重点
  新增 6 套界面风格，点右上角调色板图标循环切换，选择会记住，重启后保持。

■ ★ 主打功能：谷歌三件套修复
  国产 ROM 装上谷歌服务后"点了闪退、用了就停、重启就失效"是最常见的毛病，
  本版把这套修复集中到了「Google 服务修复中心」，一条流程修完。

  遇到的问题                          用哪个按钮
  ─────────────────────────────────────────────────────────────────
  不知道装没装、装的对不对            深度诊断环境
  装完打不开、一点就闪退              一键安装 / 更新
                                      （自动按 GSF → Play 服务 → Play 商店 排序）
  Play 服务反复"已停止运行"           修复 Play 服务停止
  重启之后又失效了                    修复重启后失效
  某个应用提示需要谷歌服务            修复应用运行
  系统有开关但找不到入口              打开谷歌服务开关
  内置包版本旧了                      导入本地三件套 / 打开最新版下载

  和"装个谷歌安装器"的区别：
    安装器只管把包推进去，装完能不能用要看系统给不给后台权限。
    这里把 安装 → 放行 → Doze 白名单 → 开机自启 → 诊断 串成一条流程。

  每个组件依次执行这五条：
    pm enable --user 0 <包名>                                  解除冻结
    cmd package install-existing --user 0 <包名>               恢复被卸掉的系统包
    cmd deviceidle whitelist +<包名>                           加入 Doze 省电白名单
    cmd appops set --user 0 <包名> RUN_IN_BACKGROUND allow     允许后台运行
    cmd appops set --user 0 <包名> RUN_ANY_IN_BACKGROUND allow 允许任意后台运行

  "修复重启后失效"额外把放行规则写进 Magisk 的 service.d
  (99_adb_gaoji_gms.sh)，每次开机自动重新放行。
  没有 Root 也不会中断，会明确提示"仅完成运行时放行"，持久化那步没成一眼可见。

  三个组件的包名：
    Google 服务框架 GSF    com.google.android.gsf
    Google Play 服务       com.google.android.gms
    Google Play 商店       com.android.vending

  注意：清理主用户数据意味着 Google 账号需要重新登录，相关操作前有二次确认。

■ 6 套风格一览
  ┌──────────┬────────────┬──────────────────────────────────────────┐
  │ 主题      │ 明暗        │ 特点                                      │
  ├──────────┼────────────┼──────────────────────────────────────────┤
  │ 深空石墨  │ 深色        │ 中性石墨灰 + 薄荷绿，平衡耐看（原深色）      │
  │ 午夜靛蓝  │ 深色        │ 深海军蓝 + 靛蓝，圆角更大，最"高级"的深色    │
  │ 曜石霓虹  │ 深色        │ 纯黑 + 青色，侧栏紫蓝渐变，科技感最强        │
  │ 清亮浅色  │ 浅色        │ 冷灰底 + 绿色强调（原浅色）                 │
  │ 暖云米白  │ 浅色        │ 米白纸面 + 深棕侧栏 + 琥珀点睛，纸质感        │
  │ 墨玉青瓷  │ 浅色        │ 低饱和灰绿 + 玉色，安静稳重                 │
  └──────────┴────────────┴──────────────────────────────────────────┘

■ 修复的问题
  · 左下角"未连接"状态在切换主题后看不清
      原因：该区域底色是浅色，文字却用了白色（侧栏色）。
      现在改用正文色，6 套主题下对比度全部达标（最差 13.3，标准要求 4.5）。
  · 导航菜单未选中项在深色主题下几乎看不见（对比度 2.22 → 8.76）
  · 导航选中项在"曜石霓虹/清亮浅色/暖云米白"下白字压亮底（2.98 → 达标）
  · "墨玉青瓷"强调色偏亮，白字对比度不足（4.20 → 5.61）

■ 功能
  与 1.0.0 完全一致，无任何功能删减或行为变化，只是多了换肤。

■ 说明
  界面风格只改配色，布局、按钮位置、操作流程都不变，可以放心切换。

■ 开源准备
  项目已在 GitHub 开源（github.com/adb-gaoji/adb-gaoji-assistant），
  补齐了贡献指南、行为准则、安全策略、第三方许可说明与 CI 工作流。
  本轮把仓库连上自动化检查后，又发现并修掉了下面这个问题。

■ 修复：投屏会话的单元测试依赖了本机文件
  现象：CI 上 tests/v20.5.8-mirror-session.test.js 稳定失败
        （AssertionError: 1 !== 0），本机却始终通过。
  原因：该测试把真实的 resources/ 目录注入给被测代码，而 start-mirror
        启动前会检查 resources/scrcpy/.../scrcpy.exe 是否存在，缺失时返回 1。
        scrcpy 体积大、不纳入版本控制（见 .gitignore），CI 克隆后该目录并不存在。
        于是一个纯粹考察"会话状态跟踪"的单元测试，
        变成了"本机是否装好 scrcpy"的隐式断言。
  修复：改用临时目录模拟出 scrcpy 已就位的前提，让测试只考察被测逻辑；
        另补一个反向用例——资源目录为空时必须返回可读的"缺少 scrcpy"提示，
        而不是抛异常或静默成功。
  结果：55/55 通过（原 54 + 新增 1），本地与 CI 两种环境一致。

  这个缺陷只在干净克隆的机器上才暴露，属于"本地怎么测都是绿的"类型，
  正是接自动化检查的价值所在。


════════════════════════════════════════════════════════════════════════════════
  【1.0.0】 2026-09-17 · 首个正式版
════════════════════════════════════════════════════════════════════════════════

  此前 20.x 为内部迭代系列，V20.5.x 的全部功能都包含在本版中。
  本版完成了一次全面的缺陷排查与工程加固。

■ 修复的问题（实际会导致手机出问题的）
  · 固件刷机"选择刷机包"失败
      原因：解析 flashfile.xml 时跳过了 <step> 标签，而摩托罗拉/联想的刷机命令
      恰好全部写在 <step operation="..."> 上，导致解析出 0 条命令后直接报错。
      现已修复，实测真实固件可正确解析出 27 条命令。
  · su/sh 命令带空格时参数被拆散（影响 23 处调用）
      现象：su -c "settings put system xxx" 这类命令会因为引号丢失而执行失败。
      现已统一做安全转义。
  · ADB 设备信息被截断，丢失机型和传输 ID
      原因：解析 devices -l 输出时用错了字符串截断参数，只保留了第一段。
      现改为按位置切分，机型信息完整。
  · 清除数据（erase）默认放行
      刷机脚本里的 erase 现在必须显式开启才会执行，避免误清用户数据。
  · Root 检测没有超时，手机异常时会卡住界面，现已加超时。

■ 工程加固
  · 日志自动轮转，不再无限增长（单文件上限 2 MB，保留 3 份）
  · 增加崩溃捕获，异常会记录到 crash.log
  · 引入 CI 流水线：语法检查 → 单元测试 → 依赖安全审计 → 7 项后台审计 → 资源校验
  · 新增 54 个自动化测试，覆盖曾经真实出过问题的边界
  · 纯逻辑模块与界面解耦（固件解析、ADB 输出解析、动作注册表），可独立测试
  · 生产依赖 0 个安全漏洞


════════════════════════════════════════════════════════════════════════════════
  【20.5.8】 · 投屏会话管理
════════════════════════════════════════════════════════════════════════════════

■ 新增
  · 投屏会话生命周期管理：能跟踪会话、拒绝重复启动、支持停止
  · 新增后台审计

■ 修复
  · 显示设置（分辨率/DPI）修改后立即生效，不需要重启手机


════════════════════════════════════════════════════════════════════════════════
  【20.5.7】 · 无线配对增强
════════════════════════════════════════════════════════════════════════════════

■ 新增
  · 无线 ADB 支持 Android 11 及以上的配对码方式
  · 配对端口与连接端口分离，避免端口冲突
  · 新增后台审计与模拟测试


════════════════════════════════════════════════════════════════════════════════
  【20.5.6】 · 无线投屏校验
════════════════════════════════════════════════════════════════════════════════

■ 新增
  · 无线投屏前增加严格校验：IP 地址每段范围、投屏端口范围
  · scrcpy 显式绑定设备（USB 序列号 或 IP:端口）


════════════════════════════════════════════════════════════════════════════════
  【20.5.5】 · 界面状态一致性
════════════════════════════════════════════════════════════════════════════════

■ 修复
  · 界面提示与后端结果不一致的问题（成功/失败/警告/处理中统一）
  · 应用和文件列表的排序、多选、筛选、滚动、刷新不再丢失当前状态


════════════════════════════════════════════════════════════════════════════════
  【20.5.4】 · 危险操作安全边界
════════════════════════════════════════════════════════════════════════════════

■ 变更
  · 刷写、解锁、卸载、清数据、清理媒体、修改显示设置全部走二次确认
  · 取消操作、设备不匹配、权限不足、备份失败都会阻止写入


════════════════════════════════════════════════════════════════════════════════
  【20.5.3】 及更早版本
════════════════════════════════════════════════════════════════════════════════

  20.5.3 / 20.5.2 / 20.5.1 / 20.5.0 / 20.4.0 / 20.3.2 及更早的逐条变更，
  请查看项目根目录的 CHANGELOG.md（开发明细）。


════════════════════════════════════════════════════════════════════════════════
  使用提示
════════════════════════════════════════════════════════════════════════════════

  · 涉及设备写入的功能（刷机、解锁、清数据、写分区）务必先核对机型和系统版本
  · 建议在操作前备份关键分区（Root 与面具页面提供一键备份）
  · 固件刷机需要先解压官方 ZIP，再选择其中的 flashfile.xml

  遇到问题可查看运行日志：
      %APPDATA%\adb-gaoji-assistant-next\
